# SIMPLE requirements.txt (universal)
gymnasium>=0.28.1
torch>=2.0.0
numpy>=1.21.0
matplotlib>=3.5.0
tqdm>=4.64.0
pandas>=1.4.0
seaborn>=0.11.0

# Platform-specific installation
# Use this for Windows (PyBullet environments)
pybullet>=3.2.5; sys_platform == "win32"

# Use this for Linux/Mac (all environments)
mujoco>=2.3.3; sys_platform != "win32"
mujoco-py>=2.1.2.14; sys_platform != "win32"
box2d-py>=2.3.5; sys_platform != "win32"
gymnasium[mujoco,box2d]>=0.28.1; sys_platform != "win32"